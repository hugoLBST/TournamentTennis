{% extends 'base.html.twig' %}

{% block title %}Tour{% endblock %}

{% block body %}
    <h1>Tour</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tour.id }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ tour.type }}</td>
            </tr>
            <tr>
                <th>DateFin</th>
                <td>{{ tour.dateFin ? tour.dateFin|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Statut</th>
                <td>{{ tour.statut }}</td>
            </tr>
            <tr>
                <th>Numero</th>
                <td>{{ tour.numero }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('tour_index') }}">back to list</a>

    <a href="{{ path('tour_edit', {'id': tour.id}) }}">edit</a>

    {{ include('tour/_delete_form.html.twig') }}
{% endblock %}
